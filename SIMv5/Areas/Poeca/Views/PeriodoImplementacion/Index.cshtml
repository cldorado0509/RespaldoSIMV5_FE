@model IEnumerable<SIM.Data.Poeca.PeriodoImplementacion>

@{
    ViewBag.Title = "POECA | Períodos de Implementación";
}
<div class="container-fluid">
    <div class="col-xs-12">

        <h2>Períodos de Implementación</h2>

        <div class="row">

            <div class="col-xs-12">
                <div class="actionButtons">
                    @Html.ActionLink("Crear Nuevo", "Crear", null, new { @class = "btn btn-default" })
                </div>

                <table class="table table-striped">

                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.NombrePeriodo)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Descripcion)
                        </th>

                        <th>Acciones</th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.NombrePeriodo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Descripcion)
                            </td>
                            <td>
                                <div class="btn-group btn-group-xs">
                                    @*@Html.ActionLink("📄", "Details", new { id = item.Id }, new { @class = "btn btn-primary", @title = "Detalles" })*@
                                    @Html.ActionLink("✏️", "Editar", new { id = item.Id }, new { @class = "btn btn-primary", @title = "Editar" })
                                    @Html.ActionLink("🗑️", "Eliminar", new { id = item.Id }, new { @class = "btn btn-danger", @title = "Eliminar" })
                                </div>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>
</div>
