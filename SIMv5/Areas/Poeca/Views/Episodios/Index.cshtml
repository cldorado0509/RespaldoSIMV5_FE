@model IEnumerable<SIM.Data.Poeca.DPOEAIR_EPISODIO>
@{
    ViewBag.Title = "POECA | Lista de Episodios";
}
<div class="container-fluid">
    <div class="col-xs-12">

        <h2>Lista de Episodios</h2>

        <div class="row">

            <div class="col-xs-12">
                <div class="actionButtons">
                    @Html.ActionLink("Crear Nuevo", "Crear", null, new { @class = "btn btn-default" })
                </div>

                <table class="table table-striped">

                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.N_ANIO)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DPOEAIR_PERIODO_IMPLEMENTACION.NombrePeriodo)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.S_OBSERVACIONES)
                        </th>
                        <th>Acciones</th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.N_ANIO)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DPOEAIR_PERIODO_IMPLEMENTACION.NombrePeriodo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.S_OBSERVACIONES)
                            </td>
                            <td>
                                <div class="btn-group btn-group-xs">
                                    @*@Html.ActionLink("📄", "Details", new { id = item.Id }, new { @class = "btn btn-primary", @title = "Detalles" })*@
                                    @Html.ActionLink("✏️", "Editar", new { id = item.ID }, new { @class = "btn btn-primary", @title = "Editar" })
                                    @Html.ActionLink("🗑️", "Eliminar", new { id = item.ID }, new { @class = "btn btn-danger", @title = "Eliminar" })
                                </div>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>
</div>
